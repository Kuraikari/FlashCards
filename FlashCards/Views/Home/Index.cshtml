@using FlashCards.Enums;
@using FlashCards.Models.FlashCards;
@model FlashCardsViewModel<FlashCardModel>

<article class="flashcard-table" role="article">
    @{await Html.RenderPartialAsync("_FlashCardsTable", Model);}
</article>